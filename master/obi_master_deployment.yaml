apiVersion: apps/v1
kind: Deployment
metadata:
  name: master
  namespace: obi
spec:
  replicas: 1
  selector:
    matchLabels:
      app: master
  template:
    metadata:
      labels:
        app: master
    spec:
      containers:
      - name: master-latest
        image: eu.gcr.io/dhg-data-intelligence-ops/master
        volumeMounts:
        - name: obi-dataproc-sa-volume
          mountPath: "/etc/sa"
        env:
        - name: GOOGLE_CLOUD_PROJECT
          value: "dhg-data-intelligence-ops"
        - name: GOOGLE_APPLICATION_CREDENTIALS
          value: "/etc/sa/dataproc-sa.json"
      volumes:
        - name: obi-dataproc-sa-volume
          secret:
            secretName: dataproc-sa
---
apiVersion: v1
kind: Service
metadata:
  name: master-service
  namespace: obi
spec:
  type: LoadBalancer
  ports:
  - port: 8080
    protocol: UDP
  selector:
    app: master